<h1>Crear una nueva Misión</h1>

	
<%=form_for @mission do |f|%>
	<%= f.label :title%></br>
	<%= f.text_field :title%></br>
	<%= f.label :content%> <span class="glyphicon glyphicon-usd"></span>
	<%= f.text_field :content%></br>
	<%= f.label :points%>
	<%= f.select :points, options_for_select([1,2,3,4,5,6,7,8,9])%></br>
	<%= f.label :take_mission%>
	<%= f.select :take_mission,options_for_select(["Si", "No"])%></br>
	<%= f.label :begin_date%>
	<%= f.text_field :begin_date%></br>
	<%= f.label :final_date%>
	<%= f.text_field :final_date%></br>
	
	<%= f.submit%>
<%end%>
